const config = {};

config.connectionString = {
	host: 'localhost',
	port: 5432,
	database: 'kpis',
	username: 'postgres',
	password: '62650205',
	dialect: 'postgres'
};

config.port = 4000;

module.exports = config;